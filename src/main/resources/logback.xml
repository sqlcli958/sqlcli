<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 第三方库只显示错误 -->
    <root level="ERROR">
        <appender-ref ref="STDOUT" />
    </root>

    <!-- com.sqlcli 包日志：默认 WARN，debug 模式通过 SQLCLI_DEBUG=DEBUG 开启 -->
    <logger name="com.sqlcli" level="${SQLCLI_DEBUG:-WARN}" additivity="false">
        <appender-ref ref="STDOUT" />
    </logger>
</configuration>